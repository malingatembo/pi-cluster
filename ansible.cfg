[defaults]
inventory = inventory.ini
host_key_checking = False
private_key_file = ~/.ssh/id_rsa

[privilege_escalation]  # FIXED SPELLING
become = True
become_method = sudo
become_user = root
become_ask_pass = False
